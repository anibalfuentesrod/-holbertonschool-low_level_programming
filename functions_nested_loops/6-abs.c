#include "main.h"
/**
 * _abs - making something with absolute value
 *
 *@num: la variable
 * Return: the value 
**/
int _abs(int num)
{
if (num < 0)
{
	return (-num);
	_putchar(num);
}
else{
	return (num);
	_putchar(num);
}

}
